# PowerShell Blue

An Omarchy theme built around the classic Windows PowerShell console color:
dark blue `#012456` surfaces with white foreground text and a solid blue
background.

Apply it with:

```sh
omarchy theme set "PowerShell Blue"
```
